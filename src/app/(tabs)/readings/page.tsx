// Placeholder do ReadingsScreen (368 linhas no app — liturgia diária, TTS,
// calendário). Ainda não portado.

export default function ReadingsPage() {
  return (
    <div style={{ padding: "var(--spacing-lg)" }}>
      <p style={{ color: "var(--color-text-muted)" }}>Leituras — em construção.</p>
    </div>
  );
}
