// Placeholder do BillsScreen (192 linhas no app — contribuições, pagamento
// PIX). Ainda não portado.

export default function BillsPage() {
  return (
    <div style={{ padding: "var(--spacing-lg)" }}>
      <p style={{ color: "var(--color-text-muted)" }}>Contribuições — em construção.</p>
    </div>
  );
}
