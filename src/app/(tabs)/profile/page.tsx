// Placeholder do ProfileScreen (128 linhas no app). Ainda não portado.

export default function ProfilePage() {
  return (
    <div style={{ padding: "var(--spacing-lg)" }}>
      <p style={{ color: "var(--color-text-muted)" }}>Perfil — em construção.</p>
    </div>
  );
}
